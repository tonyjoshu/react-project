const openButtonStyle = (mainColor) => {
  return {
    backgroundColor: "transparent",
    color: "#212121",
    border: `1px solid ${mainColor}`,
    width: "fit-content",
    padding: 0,
    paddingBlock: 5,
    paddingInline: 5,
    marginLeft: 10,
  };
};

export default openButtonStyle;
